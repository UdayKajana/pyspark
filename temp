UPDATE `your_dataset.your_table`
SET your_column = your_column + row_num
FROM (
    SELECT your_column, ROW_NUMBER() OVER () AS row_num, unique_id
    FROM `your_dataset.your_table`
) t
WHERE your_table.unique_id = t.unique_id;
